function ContainerLeft({ title, post }) {
  return (
    <div className="ourvoice__container-left">
      <h2 className="ourvoice__container-left-title">{title}</h2>
      <h1 className="ourvoice__container-left-post">{post}</h1>
    </div>
  );
}

export default ContainerLeft;
